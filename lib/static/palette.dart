import 'package:flutter/material.dart';

class Palette {
  /// Colors and text styles used throughout the app. Ensures consistency in the theme.
  static const Color light = Colors.white;
  static const Color dark = Color.fromARGB(255, 150, 150, 150);
  static const Color maroon = Color.fromARGB(255, 153, 0, 0);
  static const Color card = Color.fromARGB(245, 248, 248, 248);
}

import 'package:flutter/material.dart';

class Palette {
  /// Colors and text styles used throughout the app. Ensures consistency in the theme.
  /// TODO: remove unused colours.
  static const Color light = Colors.white;
  static const Color dark = Color.fromARGB(255, 135, 135, 135);
  static const Color maroon = Color.fromARGB(255, 128, 0, 0);
  static const Color card = Color.fromARGB(205, 248, 248, 248);
  static const Color divider = Color.fromARGB(225, 230, 230, 230);
  static const Color tableLight = Color.fromARGB(240, 243, 243, 243);
  static const Color buttonText = Color.fromARGB(250, 200, 200, 200);
  static const Color tablePlayer = Color.fromARGB(255, 100, 248, 248);
  static const Color textInMaroon = Color.fromARGB(255, 230, 230, 230);
}
